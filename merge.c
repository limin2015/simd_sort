/**********
 merge two sorted array into one sorted array.
 using traditional method.
 *********/
 void merge(int *a, int *b, int *ret,int len){
		int a_pos = 0;
		int b_pos = 0;
		int i;
		while(a_pos < len && b_pos < len){
			
		}
 
 }
